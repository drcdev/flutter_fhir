name: flutter_fhir
description: Trying to HTTP

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_cupertino_localizations: ^1.0.1
  http: ^0.12.0+2
  json_annotation: ^2.4.0
  cupertino_icons: ^0.1.2
  grouped_buttons: ^1.0.4
  shared_preferences: ^0.4.3
  path_provider: ^0.4.1
  random_string: ^1.1.0
  intl: ^0.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.0.0
  json_serializable: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - images/
    
